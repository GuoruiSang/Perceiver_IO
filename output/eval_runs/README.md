# eval_runs

Runtime output location for active evaluation runs.

The new experiment contract writes per-run metrics, manifests, and trajectories here.
Old `output/raw_runs/` outputs were removed after the pipeline was renamed.
